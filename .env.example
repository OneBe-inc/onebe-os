# These are server-only runtime settings. Never add a VITE_ prefix.
# sites mode is valid only behind the trusted Sites dispatcher.
POLICIES_AUTH_MODE=
POLICIES_APPROVED_FOLDER_ID=
POLICIES_DRIVE_ID=
# Configure this as a hosted secret, not a checked-in file or browser input.
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=
